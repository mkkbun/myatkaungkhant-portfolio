# Contact form — Gmail SMTP (messages arrive at CONTACT_TO_EMAIL)
# Create an App Password: https://myaccount.google.com/apppasswords
# (requires 2-Step Verification on your Google account)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=myatkaungkhant022@gmail.com
SMTP_PASS=your_16_character_app_password
CONTACT_TO_EMAIL=myatkaungkhant022@gmail.com

# API server port (default 3001)
PORT=3001

# Allowed frontend origin(s), comma-separated
CLIENT_ORIGIN=http://localhost:3000

# Production only: set if the API runs on a different host than the website
# VITE_API_URL=https://your-api.example.com
